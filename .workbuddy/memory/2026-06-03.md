# 2026-06-03 工作日志

## 音频时间轴修复（完整流程，共4版迭代）

### 问题
"修复音频时间轴"弹窗：指定节时无法加载节列表 → 修复时全部跳过

### 迭代修复过程
1. **v1** - 后端缺 `/books/{id}/sections` 路由 + 前端状态未恢复
2. **Git凭据过期** → 配置 GitHub Personal Access Token 解决推送问题
3. **v2** - audio_duration 全为0 → 从磁盘MP3读取真实时长（纯Python解析帧头）
4. **v3** - 所有segment指向同一音频文件 → 改为按字符位置比例从整节总时长分配绝对时间
5. **v4** - v2遗留旧char_timeline阻止重新生成 → 增加 force 强制覆盖模式 ✅ 最终成功

### 最终方案
- 读取该节所有 TTS 分块音频子文件（section_{id}_*.mp3），计算总时长
- 按 text_segment 的 start_char/end_char 在全文中的比例，分配绝对时间范围
- 段内按标点权重生成逐字符时间点
- 前端默认传 force=true 覆盖旧数据

### 验证结果（节662）
4/4 segments 全部修复成功，总时长32.7s正确分配到各段
