# 2026-06-04 工作日志

## 修复微信内置浏览器音频播放问题

**问题**：项目在手机微信内浏览时，阅读中的点评、小结音频都没有播放出来。

**原因**：微信内置浏览器严格限制音频自动播放，必须由用户手势触发。`_annotationAudio.play()` 是在原文音频的 `onended` 回调中触发的，没有用户手势，被微信直接拦截。

**修复方案**：在 `player.init()` 中添加了微信音频解锁逻辑：
- 监听 `touchstart` / `touchend` / `mousedown` / `click` 首次用户手势
- 对手势触发时，对 `audio` 和 `_annotationAudio` 各做一次 `play()→pause()` 预热
- 解锁后，后续在 `onended` 回调中调用 `play()` 就能成功

**修改文件**：`frontend/index.html` — `player.init()` 函数

**状态**：代码已修改，待部署验证

---

## 微信音频修复 v2：手势补播方案

**根因**：微信浏览器里 `onended`/`oncanplay` 回调不是用户手势，`_annotationAudio.play()` 被静默拦截。预热解锁只对"同一个手势上下文"有效，对 onended 回调无效。

**修复方案**：
- `init()` 中包装 `_annotationAudio.play()` 方法，捕获 reject 后注册 `touchstart`/`click` 手势补播
- `_resetAnnotationAudio()` 不再重建 Audio 实例（保留解锁状态）
- `_finishInsertPoint()` 移除 `cloneNode` 重建逻辑

**commit**: 已提交并推送

---

## 点评/小结插播时禁用播放按钮

**需求**：点评/小结播放期间禁用播放按钮，防止用户误操作播放/停止

**修改**：
- `_playInsertPoint()`：新增禁用 `playBtn`（与思考按钮同步置灰）
- `_finishInsertPoint()`：新增启用 `playBtn`（与思考按钮同步恢复）

**commit**: 7e6bbce（已推送）

---

## 已读节主线播放时移除点评高亮残留

**问题**：已读节重新播放时，原文逐字显示到点评引用处时出现了金色波浪下划线。这是已读节加载时 `_highlightAnnotation()` 添加的 `annotated` 高亮类残留。

**修复**：在 `revealCharsUpTo()` 中，每次显示字符时主动清除 `annotated` 类。

**为什么安全**：
- 主线播放时：`_updateDisplayByTime()` → `revealCharsUpTo()` 会清除 annotated，确保无高亮
- 点评插入播放时：`_updateDisplayByTime()` 因 `_currentSegment.type !== 'text_segment'` 跳过，不调用 `revealCharsUpTo()`，点评高亮不受影响

**commit**: a9f0afb（已推送）

---

## 评论区间轮巡在点评/小结未播完就切走的问题

**问题**：点评或小结篇幅较大时，音频还在播放，评论区的标签轮巡就切到「名言赏析」去了。

**根因**：`addAnnotationTab` 中用固定 10 秒 setTimeout 恢复轮巡——长点评音频播完前定时器先触发。

**修复**（`addAnnotationTab`）：
- `isPlayed=true`（已读节，无音频播放）：保留 10 秒定时器恢复轮巡
- `isPlayed` 为 falsy（阅读中的新点评）：不设定时器，由 `_finishInsertPoint` 在音频播完后恢复轮巡

**commit**: 1be244b（已推送）

---

## 点评/小结播放期间禁用标签点击 + 内容溢出自动滚动

**需求1**：轮巡暂停时（插入播放中），禁止用户点击其他标签；轮巡恢复后才允许
**修改**：`renderTabs()` 的 onclick 开头检查 `state.isPlayingAnnotation`，true 则直接 return

**需求2**：点评/小结篇幅超出评论区时，内容逐字显示时自动滚到底部
**修改**：两处 timeupdate 处理器末尾添加：
```javascript
var analysisArea = document.getElementById('analysisArea');
if (analysisArea) { analysisArea.scrollTop = analysisArea.scrollHeight; }
```
- `_playCommentAudio` 的 `commentTimeUpdateHandler`
- `_playSummaryContent` 的 `timeUpdateHandler`

**commit**: be92691（已推送）

---

## 宣传区域内容更新 + 样式美化 + 同步到书架页

**需求**：更新公开浏览页宣传内容为4行新文案，美化显示效果，同步放置到登录后的书架目录页。

**公开浏览页（publicPromotion）修改**：
- 第一行：✨ 超级实用的阅读辅助工具 → 孩子阅读，费脑不费力！
- 第二行卡片：💬 精彩评论不断/思考AI评分 + 培养阅读习惯/提升阅读兴趣
- 第三行卡片：🏅 军衔等级晋升/军功表彰加持 + 增加阅读趣味性/激发孩子荣誉感
- 第四行：—— 中外名著，公版书籍 ——

**样式美化**：双卡片 grid 布局、金色渐变标题、emoji 图标装饰、虚线分隔、阴影边框

**书架页（homePromotion）新增**：在 bookList 下方添加相同内容的宣传区域（内嵌于 homePage 内，非 fixed 定位）

**commit**: 754751a（已推送）

---

## 宣传区域细节修复

- 文案修正："费脑不费力" → "只费脑不费力"
- 双卡片布局精简：每卡片从 4 行缩为 2 行（emoji + 两句话）
- 底部适配：`publicPage` 的 `bottom` 从 140px 扩大到 215px，`padding-bottom` 从 160px 缩为 30px，确保宣传栏完整可见

**commit**: 8439c72（已推送）

## 图标左置 + 底部空间增大

- 双卡片图标从文字上方移到左侧（`display:flex;align-items:center`），视觉更紧凑
- `publicPage` 底部预留从 200px 增至 215px

**commit**: 14cf76d（已推送）

## 宣传卡片文案优化

- "培养阅读习惯，提升阅读兴趣" → "引导随读思考，默化兴趣养成"
- "增加阅读趣味性，激发孩子荣誉感" → "增加阅读趣味，激发孩子荣誉"

**commit**: b3c8dae（已推送）

## 微信浏览器底部安全区适配

**问题**：宣传区在微信内置浏览器中底部被微信导航栏遮挡，显示不全。

**修复**：
- 宣传区底部 padding 改为 `calc(14px + env(safe-area-inset-bottom, 50px))`，为微信底部栏预留空间
- `publicPage` 底部预留从 215px 增至 250px
- viewport meta 添加 `viewport-fit=cover`，使 iOS 安全区 API 生效

**commit**: c17b465（已推送）

## 修复用户信息/密码/留言页滚动失效

**根因**：三个页面的内联 `min-height: 100vh` 与 `.page` 基类的 `flex: 1` 产生冲突。flex 容器中 `flex: 1` 配合 `min-height: 0` 才能让 `overflow-y: auto` 正常触发滚动；`min-height: 100vh` 强制撑满视口导致 flex 无法正确计算溢出，滚动失效。

**修复**：移除 `userInfoPage`、`userPasswordPage`、`userMessagePage` 内联的 `min-height: 100vh`，让基类 `flex: 1; min-height: 0; overflow-y: auto` 正常生效。

**commit**: 4ead461（已推送）

## 书架页布局重构 — 宣传区固定底部

**问题**：书架页（homePage）中书籍列表和宣传区纵向排列，书多时宣传区被推出屏幕。

**修复**：将 homePage 改为三段式 flex 布局：
- `homePage`: `display:flex;flex-direction:column`（继承自 `.page.active`）
- `bookList`: `flex:1;overflow-y:auto;min-height:0` — 撑满剩余空间，独立滚动
- `homePromotion`: `flex-shrink:0` — 固定在底部，不被压缩

**commit**: 0b39b36（已推送）

## 书架页改为 fixed 定位布局

**根因**：上一步虽改为 flex 布局，但 homePage 仍在 `.container`（min-height:100vh, flex-shrink:0）内部，容器只会撑大不会收缩，promotion 仍被推出。

**修复**：CSS 新增 `#homePage.active` 规则，使用 `position: fixed; top: 52px; bottom: 0;` 脱离容器流，直接占满导航栏下方到屏幕底部，内部 bookList 独立滚动，promotion 固定底部。

**commit**: e922b7f（已推送）

---

## 管理页面拆分：admin.html 独立

**需求**：将管理页面从 index.html 中完全拆分出来，原因是：(1) 管理页面打不开 (2) 管理功能仅管理员使用，不应混入读者主页面。

**完成工作**：
1. 创建 `frontend/admin.html`：独立管理页面，包含完整 HTML/CSS/JS、登录验证、7 个管理标签页
2. 删除 `index.html` 中所有管理员代码：
   - CSS：管理员样式（~80行）
   - HTML：`adminPage` div + `editModal` 编辑弹窗
   - JS：`admin` 对象、`previewBookIcon`、`adminSaveBook`、`adminDeleteBook`、`closeEditModal`、`adminLoadBooks`、`switchAdminTab` 及所有管理函数（`loadQuotes`、`loadAdminMessages`、`searchAdminBooks`、`searchAdminUsers`、`toggleUserRole`、`resetUserPassword`、`clearUserDevice`、`adminShowCatalogStats`、`renderAdminChapters`、`reimportChapter`、`reimportSection`、`fixSectionAudio`、`pollFixProgress`、`pollTTSProgress`、`searchAdminSubscriptions`、`adminRemoveSub` 等）
3. 入口点更新：个人中心和浏览页的管理按钮改为 `window.location.href='admin.html'`
4. `showPage()` 中移除 adminPage 处理分支
5. 文件从 ~9068 行缩减到 ~7328 行

**状态**：代码已完成，待 commit & push
