# 悦读小将 - 项目需求文档

> 本文档随项目迭代持续更新。每次修改前需对照本文档，确保不违背已有需求。

---

## 一、核心概念

### 1.1 书签（Bookmark）
书签是用户阅读进度的唯一权威记录。

**定义：**
- 书签记录 `current_section_id`（当前节ID）和 `current_position`（主线字符位置）
- 书签只跟踪主线位置，不受子过程（点评原文引用、小结）影响

**书签位置含义：**
| 书签位置 | 节的阅读状态 |
|---------|-------------|
| 书签不在本节，且书签在后续节 | 本节已读 |
| 书签不在本节，且书签在前面节 | 本节未读 |
| 书签在本节开头或中间 | 本节在读 |
| 书签在本节末尾（position >= end_char） | 本节已读 |

**节状态判断逻辑（书签是唯一权威来源）：**
```
progress = 书签
sectionEnd = 节末尾位置

if (书签节ID > 当前节ID) → 已读
else if (书签节ID < 当前节ID) → 未读
else if (书签节ID == 当前节ID && 书签位置 >= 节末尾) → 已读
else if (书签节ID == 当前节ID && 书签位置 < 节末尾) → 在读
```

### 1.2 主线与子过程
- **主线**：节的正文内容文本
- **子过程**：点评原文引用、点评评论、小结
- 书签只记录主线位置，子过程播放不影响书签

### 1.3 章节阅读控制
- 用户必须按顺序阅读节（读完上一节才能读下一节）
- 只有书签所在的节是"在读"状态，其他未读节不可进入

---

## 二、节状态与显示

### 2.1 已读节
- 显示全部文字（revealAll）
- 显示所有点评和小结标签
- 启动标签轮巡
- 点击播放：从头重播

### 2.2 在读节
- 按书签位置显示到对应文字
- 显示书签
- 不预显示点评和小结（由播放进度触发）
- 启动标签轮巡
- 点击播放：从书签位置继续

### 2.3 未读节
- 不显示点评、小结和序号标记
- 启动标签轮巡
- 不允许进入（除非是第一节）

---

## 三、音频播放流程

### 3.1 进入阅读页
1. 显示加载提示（居中遮罩 + 进度 + 计时）
2. 预加载所有音频段
3. 加载完成前禁用播放按钮
4. 加载失败：禁用播放按钮 + 显示错误提示

### 3.2 播放控制
- 播放前检查：按钮是否被禁用、是否有音频缺失警告
- 音频加载失败时：完整清理状态（isPlaying、segmentTimeout），不触发段结束，避免错误标记已读
- 音频 onerror：不调用 _onSegmentEnd()，显示错误提示让用户刷新

### 3.3 播放队列结束
- 书签保存到节末尾位置（不是总字数）
- 标记为已读
- 恢复名言赏析

---

## 四、断点保存

### 4.1 保存时机
- 播放过程中定时保存（每10秒）
- 离开阅读页时保存
- 切换到其他节时保存

### 4.2 保存内容
- current_section_id
- current_position（主线字符位置）
- current_segment_id（当前音频段ID，可选）
- audio_position（段内音频时间，可选）

### 4.3 不保存的情况
- 已读节重播时不保存断点

---

## 五、赏析区（点评与小结）

### 5.1 标签类型
- 名言赏析（默认）
- 点评标签（按序号显示）
- 小结标签

### 5.2 轮巡规则
- 已读节：显示所有标签，启动轮巡
- 在读节：启动轮巡，点评/小结由播放进度触发显示
- 未读节：只有名言赏析，启动轮巡

### 5.3 再次阅读同一节
- 重置播放状态而不是跳过加载
- 重置 _playlistIndex、_currentSegment、_insertPointStep
- 重置音频状态
- 调用 reader.resetDisplay()

---

## 六、音频相关

### 6.1 TTS 声音
- 默认使用 person=3（度逍遥，男声）
- 充值后如果声音变化，检查百度TTS服务

### 6.2 音频预加载
- 进入阅读页时预加载所有音频
- 显示加载进度（x/y）和耗时
- 加载完成自动消失
- 加载失败禁用播放按钮

---

## 七、开发规范

### 7.1 版本号
- 手动维护在 `backend/main.py` 的 `CURRENT_VERSION` 变量
- 格式：`#数字`（如 `#294`）
- 每次修改后更新版本号，确保与对话中提到的一致
- 服务器部署后可通过 `/health` API 确认版本

### 7.2 代码修改
- 修改前对照需求文档
- 考虑对全局的影响
- 涉及冲突时与用户确认

### 7.3 日志
- 保持必要的调试日志
- 上线后移除过多日志

---

## 八、问题修复记录

### 8.1 常见问题
- [x] 节状态判断：使用书签作为唯一权威来源
- [x] 音频播放失败：完整清理状态，不错误标记已读
- [x] 再次阅读同一节：重置播放状态
- [x] 预加载提示：显示进度和计时
- [x] 版本号：自动从git获取

### 8.2 待观察
- 音频加载失败的根因（文件是否存在）

---

# 当前版本：#295
